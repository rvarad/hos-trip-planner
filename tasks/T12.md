# T12: Deploy

**Goal:** Docker on OCI ARM VM (open port 8000 on Security List), Vercel frontend targeting bom1 region, environment variables set, smoke test passing.

## Progress

- [x] **Backend on OCI ARM VM** — built the backend image on the VM (native
      arm64), runs via `docker compose` with gunicorn (3 workers) on `:8000`. Prod
      env in `.env` (gitignored): real `SECRET_KEY`, `DEBUG=false`, and
      `ALLOWED_HOSTS` set to the VM's public IP. Port 8000 opened in the OCI Security
      List + VM firewall. Verified externally: GET → 405, and a real
      Chicago→Dallas→Houston POST → 200 with `routing=osrm`, ~1205 mi, 2 days,
      full route geometry.
- [x] **Reserve the VM's public IP** — confirm the VM's public IP is _reserved_
      (not ephemeral) so `BACKEND_URL` doesn't break on reboot.
- [x] **Vercel frontend** — import repo (root = `frontend`), set `BACKEND_URL`
      (the VM origin) as a Vercel **secret/env var**, region `bom1`, deploy, smoke
      test.
- [ ] **Self-host Photon** (optional/later): run Photon in Docker on the OCI VM
      with a U.S. OSM index, then point `PHOTON_URL` at it (replacing the public
      `photon.komoot.io` instance used during development). See T5 ADR and T7.
