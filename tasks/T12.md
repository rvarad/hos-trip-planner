# T12: Deploy

**Goal:** Docker on OCI ARM VM (open port 8000 on Security List), Vercel frontend targeting bom1 region, environment variables set, smoke test passing.

Subtasks: TBD — detail before starting. Must include:
- **Self-host Photon**: run Photon in Docker on the OCI VM with a U.S. OSM index,
  then point `PHOTON_URL` at it (replacing the public `photon.komoot.io` instance
  used during development). See T5 ADR and T7.
