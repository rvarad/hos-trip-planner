# T11: Polish

**Goal:** Error states, loading indicators, mobile layout, and README updates.

More subtasks TBD — detail before starting. Known items so far:
- **Form-bar clutter (from T6.1):** each `LocationField` shows two text buttons
  ("Drop pin", "Use my location"), so three fields crowd the top bar and squeeze the
  inputs (labels truncate). Convert these to compact icon buttons (needs an icons
  dep, e.g. `@mui/icons-material`) and give the location inputs more width.

## Subtasks

- [x] **T11.1** Round the displayed total miles to 2 decimals
  - `TripPlanner` summary uses `total_miles.toFixed(2)` (was raw float, e.g.
    `3386.4283179467128`).
  - Test: a fractional total renders with exactly two decimals.

- [x] **T11.2** Place mid-leg stop markers on the route line
  - Mid-leg stops (fuel, rest, break) were linearly interpolated between leg
    endpoints (the straight chord), so they floated off the curved road route on
    the map. `_point_in_leg` now interpolates along the leg's actual geometry by
    distance fraction; legs without geometry fall back to endpoint interpolation.
  - Test: a leg whose geometry bends off its chord places a mid-leg point on the
    bend, not on the chord.
