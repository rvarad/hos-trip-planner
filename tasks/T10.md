# T10: Map display with markers

**Goal:** Show the route on a map with markers for pickup, drop-off, rest breaks,
and fuel stops. Reuses the `<MapView>` component from T6a (display mode, no pin
picking) so input and result share one map implementation.

Subtasks: TBD — detail before starting. Will consume the route geometry from the
routing client (T5.2) and the stop markers from the engine timeline.
