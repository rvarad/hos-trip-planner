# T8: Log-sheet SVG rendering

**Goal:** Render one ELD daily log per day as an SVG — duty-status line on the standard 24-hour grid plus per-status totals.

## Subtasks

- [x] **T8.1** Static SVG grid
  - Renders 24-hour columns, 4 status rows, labels, quarter-hour ticks. Pure presentational, no data.
- [x] **T8.2** Duty-status line + totals
  - Accept a day's segments as props, draw the continuous line, compute and display per-status totals.
- [x] **T8.3** Wire into TripPlanner
  - Render `<DailyLogSheet>` for each entry in `planResult.days`.
